services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: demy_store
      POSTGRES_USER: demy
      POSTGRES_PASSWORD: demy_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
